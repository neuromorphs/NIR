@Library("jenlib") _

withCcache() {
	wafDefaultPipeline(projects: ["nir"],
	                   container: [app: "dls-core"],
	                   configureInstallOptions: "--build-profile=ci",
	                   notificationChannel: "#softies")
}
