@Library("jenlib") _

try {
	runOnSlave(label: "frontend") {
		stage("Checkout") {
			checkout scm
		}

		pipelineFromMarkdown(markdownFilePath: "INSTALL.md",
		                     blockType: "shell")
	}
} catch (Throwable t) {
	notifyFailure(mattermostChannel: "#dls-software")
	throw t
}
